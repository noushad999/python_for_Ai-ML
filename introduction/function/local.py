def message():
  #local variable
  choice="hello world"
  print(choice)

message()
# print(choice) #this will give an error because choice is a local variable and cannot be accessed outside the function.