# --------------------------------------------------------------

# 📝 OOP (Object-Oriented Programming)

# --------------------------------------------------------------

# 🔶 BEFORE OOP – পুরোনো স্টাইল (Procedural Programming)

# --------------------------------------------------------------

# • Code মূলত একের পর এক function ও variable নিয়ে লেখা হতো।

# • Data আলাদা, function আলাদা—একা-একা কাজ করত।

# • Program বড় হলে:

# - কোড বোঝা কঠিন হতো

# - একই code জায়গায় জায়গায় repeat করতে হতো

# - Data কে কে পরিবর্তন করছে বোঝা যেত না

# - Bug fix করা ও maintain করা কঠিন হয়ে যেত

# উদাহরণ:

# user-এর name ও age store করতে আলাদা variable

name = "Rahim" age = 25

def show_user(n, a): print("Name:", n, "Age:", a)

show_user(name, age)

# ---- সমস্যা ---

# • User বাড়লে name1, name2, age1, age2 এভাবে অনেক variable লাগে

# • Data + Function আলাদা বলে ভুল বেশি হয়

# --------------------------------------------------------------

# 🔶 কেন OOP এলো? (মূল সমস্যা)

# --------------------------------------------------------------

# 1) Data এবং Function আলাদা থাকার কারণে ভুল হওয়া

# 2) বড় প্রোজেক্টে code পুনরায় ব্যবহার করা কঠিন

# 3) Code অনেক repetitive হয়ে যেত

# 4) Real-life জিনিসগুলোর মত structure করা যেত না

# 5) Maintenance খুব কঠিন হয়ে যেত

# --------------------------------------------------------------

# 🔶 AFTER OOP – Object-Oriented Programming

# --------------------------------------------------------------

# OOP এ Data + Function = একত্রে রাখা হয় → Class-এর ভিতরে

# Class হলো  Blueprint

# Object হলো সেই blueprint থেকে তৈরি করা বাস্তব জিনিস

# সুবিধা:

# • Data + Function একসাথে → কম ভুল

# • Code reusability → Inheritance, Polymorphism

# • Code clean ও organized

# • বড় project maintain করা সহজ

# • Real-life model করা সহজ

# --------------------------------------------------------------

# 🔶 CORE CONCEPT of OOP

# --------------------------------------------------------------

# 1) Class → নীলনকশা (Design)

# 2) Object → Class থেকে তৈরি বাস্তব ভ্যালু

# 3) Encapsulation → Data গোপন রাখা (getter/setter)

# 4) Inheritance → Code reuse করা

# 5) Polymorphism → একই কাজ অনেকভাবে করা

# 6) Abstraction → জটিল জিনিস লুকিয়ে শুধু দরকারি অংশ দেখানো

# --------------------------------------------------------------

# 🔶 VERY SIMPLE EXAMPLE

# --------------------------------------------------------------

class User: # Constructor def **init**(self, name, age): self.name = name
self.age = age

    # Method
    def show(self):
        print("Name:", self.name, "| Age:", self.age)

# Object তৈরি

u1 = User("Rahim", 25) u2 = User("Karim", 30)

u1.show() u2.show()

# --------------------------------------------------------------

# ফলাফল:

# BEFORE:

# - name1, name2, age1, age2 → অনেক variable

# - Function আলাদা → ভুল হবার সম্ভাবনা বেশি

#

# AFTER (OOP):

# - User class → সব তথ্য + Function এক জায়গায়

# - Object → যেকোনো সংখ্যক user তৈরি করা যায়

# --------------------------------------------------------------


